# Create runtime

apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc

# Apply to a pod

k run secure --image=nginx --dry-run=client -o yaml > pod.yaml

Add under spec: 

-- runtimeClass: gVisor


# Verify

k exec secure -- dmesg

## You'll see gVisor starting


