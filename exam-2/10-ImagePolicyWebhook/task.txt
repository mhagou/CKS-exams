# Question

We are required to deploy an ImagePolicyWebhook admission controller to secure the deployments that are made on 
our cluster by ensuring the below:

Update and fix the issue on the /etc/kubernetes/pki/admission_configuration.yaml which will be used by 
ImagePolicyWebhook.
Ensure that the policy is set to implicit deny.
Ensure the plugin is on the kube-api server

